name: Domain Radar

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch: {}

concurrency:
  group: radar
  cancel-in-progress: true

jobs:
  scan:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - shard: prime
            tlds: ".com,.dev"
          - shard: tech
            tlds: ".io,.app,.ai"
          - shard: alt
            tlds: ".co,.sh,.xyz"
          - shard: fun
            tlds: ".cool,.lol"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node src/index.js --max-runtime 2700000 --tlds ${{ matrix.tlds }}
      - uses: actions/upload-artifact@v4
        with:
          name: results-${{ matrix.shard }}
          path: data/

  merge:
    needs: scan
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: results-prime
          path: shards/prime/data
      - uses: actions/download-artifact@v4
        with:
          name: results-tech
          path: shards/tech/data
      - uses: actions/download-artifact@v4
        with:
          name: results-alt
          path: shards/alt/data
      - uses: actions/download-artifact@v4
        with:
          name: results-fun
          path: shards/fun/data
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/merge-shards.js
      - run: node scripts/encrypt-data.js
        env:
          RADAR_PASSWORD: ${{ secrets.RADAR_PASSWORD }}
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/checked.json docs/data/results.enc.json docs/data/status.enc.json
          git diff --staged --quiet || git commit -m "Update radar results" && git push
